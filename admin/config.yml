backend:
  name: git-gateway
  branch: main
  identity_url: "https://historiasdeamoryconexiones-com.netlify.app/.netlify/identity"
  gateway_url: "https://historiasdeamoryconexiones-com.netlify.app/.netlify/git"


<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Content Manager</title>

    <!-- Decap CMS -->
    <script src="https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"></script>

    <!-- Netlify Identity Widget -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    <!-- Netlify API Connect -->
    <script>
      if (window.netlifyIdentity) {
        window.netlifyIdentity.init({
          APIUrl: "https://historiasdeamoryconexiones-com.netlify.app/.netlify/identity"
        });
      }
    </script>
  </head>
  <body>
  </body>
</html>
